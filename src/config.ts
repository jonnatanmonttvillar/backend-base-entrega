export const configuration = {
    username: process.env.USERNAME ?? "COMO PASAR CONFIGMAP DE KUBERNETE AQUI",
    port: process.env.PORT ?? 3001,
    apiKey: process.env.API_KEY ?? "COMO PASAR CONFIGMAP DE KUBERNETE AQUI"
}
